这题写过类似的，写的时候错把他当成写一个快速找到最小值的队列了。
其实换成本题的栈道理也是一样的，就是建立一个辅助栈，里面存最小值。
当然push操作的时候关于temp的add 符号带不带等于是一个注意点。
实现如果不带等号，例如1 2 1，先将三个数字push操作，然后再pop一下。
因为没有等号，temp中只有2 1，而这次pop时，本意是pop第二个1，
但是会错误的将第一个1从temp中pop出来，会导致程序出问题。