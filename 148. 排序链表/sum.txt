没有思路。链表排序不同于数组排序，链表无法随机读取，只能线性读取。
这题重点是要通过快慢指针找到链表的中间点，中间点为slow指针所在点，然后通过slow next = null，将原链表分成两个子链表，这样下面再递归调用，就是归并的常规操作了。
其实我一开始也想到寻找中间节点的做法，但是没想到要用快慢指针。
但是这个是递归版的写法，题目要求是空间复杂度要在常数级，所以要改成非递归的，因为递归调用栈会又logn的空间开销。
非递归的写法暂时没写，有空写。
